# Notion KB Manager - Docker Environment Variables
# Copy this to .env.docker and fill in your actual values

# API Keys (REQUIRED)
NOTION_API_KEY=your-notion-api-key-here
OPENAI_API_KEY=your-openai-api-key-here

# Encryption Key (REQUIRED)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-encryption-key-here

# Optional: Override default secrets (recommended for production)
# SECRET_KEY=your-custom-secret-key-min-32-chars
# JWT_SECRET_KEY=your-custom-jwt-secret-min-32-chars
# DB_PASSWORD=your-custom-postgres-password
